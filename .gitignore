.DS_Store
__pycache__/
*.pyc

# API tokens — never commit the real .env, only .env.example
.env

# User-specific config overrides (do not commit — use *.default.yaml as the base)
config/config.yaml
config/n_config.yaml
config/plots_config.yaml

# Outputs
outputs/
.snakemake/
logs/
memory/
text_docs/
# Resources
resources/

# Local PyPSA-Eur soft-link test data (solved network + regions GeoJSON) —
# not part of the repo; point pypsa_eur_link.network_path/regions_path at
# your own copy.
pypsa-eur/

# Claude Code project instructions (local only)
CLAUDE.md
